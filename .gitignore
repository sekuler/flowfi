node_modules
.env
dist
activity-script.cjs
register-entity.cjs
register-entity-secret.cjs
test-circle.cjs
recovery_file*.dat
temp_swap.txt

# Foundry — build artifacts and dependencies, both reproducible via
# `forge build` / `forge install foundry-rs/forge-std --no-git --no-commit`
out/
cache/
broadcast/
lib/
